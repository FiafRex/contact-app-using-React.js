 import React from "react";

const Header = ()=>{
    return(
        <div className = "ui fixed menu">
            <div className = "ui containter center">
            <h2 className="header-content">Contact Manager</h2>
            </div>
        </div>

    );
};

export default Header;